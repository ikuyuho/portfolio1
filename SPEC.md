# ポートフォリオサイト 仕様書

## 目的

副業・フリーランス営業のための実績紹介サイト。
クライアント・発注者に業務改善実績を見せ、案件獲得につなげる。

---

## 想定読者

| 読者 | 主な関心事 |
|------|-----------|
| クライアント・発注者 | 自分の業務に適用できるか、信頼できる実績があるか |

訪問者は技術的な詳細より「自分の仕事がどう改善されるか」を重視するため、
定量的な成果（数字・効果）を前面に出す。

---

## サイト構成

シングルページ構成（ワンスクロールで完結）。

```
[ヒーロー] 名前・肩書き・キャッチコピー
    ↓
[業務改善事例] カード一覧
    ↓
[お問い合わせ] 連絡先・SNSリンク
```

ページ分割はせず、セクションをアンカーリンクで繋ぐ。

---

## 技術方針

### 確定スタック

| 項目 | 採用技術 |
|------|---------|
| フレームワーク | **Next.js 15 (App Router)** |
| 言語 | **TypeScript** |
| スタイリング | **Tailwind CSS v4** |
| 出力方式 | 静的エクスポート（`output: 'export'`） |
| ホスティング | **Vercel**（無料枠） |

### 採用理由

- 本業で Next.js + TypeScript を使用しており、学習コストゼロ
- シングルページ構成なので `app/page.tsx` 1ファイルのみ。ルーティングの複雑さは生じない
- 事例カードをコンポーネント（`CaseCard.tsx`）として型安全に実装できる
- Vercel への Next.js デプロイが最もシンプル

### 事例データの管理方法

TypeScript の型付き配列として `src/data/cases.ts` で管理する。
Markdown/CMS は使わず、コードを直接編集して更新する。

```ts
// src/data/cases.ts
export type Case = {
  id: string;
  title: string;
  description: string;
  result: string;
  tools: string[];
};
```

---

## ディレクトリ方針

```
portfolio1/
├── public/                  # favicon, OGP画像
├── src/
│   ├── app/
│   │   ├── layout.tsx       # ルートレイアウト（フォント・メタデータ）
│   │   ├── page.tsx         # メインページ（唯一のページ）
│   │   └── globals.css      # Tailwind ベースCSS・カラートークン
│   ├── components/
│   │   ├── Hero.tsx
│   │   ├── CaseCard.tsx
│   │   ├── CaseList.tsx
│   │   └── Contact.tsx
│   └── data/
│       └── cases.ts         # 事例データ（型付き配列）
├── SPEC.md
├── next.config.ts
├── tailwind.config.ts
└── package.json
```

---

## デザイン方針

### テーマ: ウォーム・和預き

| 要素 | 方針 |
|------|------|
| ベースカラー | ベージュ・アイボリー（#FAF7F2 など） |
| アクセントカラー | アンバー・テラコッタ系 1色のみ |
| テキスト | ウォームグレー（冷たい純黒を避ける） |
| カード | 角丸あり、薄いシャドウ |
| フォント | 日本語: Noto Serif JP または BIZ UDPGothic |
| アニメーション | 使用しない（シンプルに保つ） |
| レスポンシブ | モバイルファースト |

---

## 事例カードの項目

1 事例 = 1カード。カードに表示する内容:

| 項目 | 説明 | 表示例 |
|------|------|-------|
| 案件名 | 事例のタイトル | 「請求書処理の自動化」 |
| 内容説明 | 何をどう改善したかの概要（2〜3行） | 「月200件の手入力作業をRPAで自動化…」 |
| 定量的成果 | 数字で示した効果 | 「作業時間 80% 削減 / 月40時間の削減」 |
| 使用ツール・技術 | 実装に使ったもの | `UiPath` `Excel VBA` `Python` |

---

## 不要な機能

以下は意識的に含めない:

- ブログ・記事投稿機能（CMS不要、静的コンテンツで十分）
- アニメーション・スクロールエフェクト（シンプルさを優先）
- ダークモード切り替え（テーマを固定）
- 多言語対応（日本語のみ）
- 検索・フィルター機能（事例数が少ないため不要）

---

## 開発フェーズ（案）

| フェーズ | 内容 |
|---------|------|
| 1 | プロジェクト初期化・デザイントークン定義 |
| 2 | ヒーローセクション実装 |
| 3 | 事例カードコンポーネント実装 |
| 4 | 事例データ（`cases.ts`）の登録 |
| 5 | お問い合わせセクション実装 |
| 6 | レスポンシブ調整・デプロイ |
